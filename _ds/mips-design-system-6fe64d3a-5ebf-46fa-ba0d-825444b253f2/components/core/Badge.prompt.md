Small uppercase pill label for a status or category tag.

```jsx
<Badge tone="maize">Open Enrollment</Badge>
<Badge tone="navy">Grades K-5</Badge>
```

Tones: `navy` (filled, default emphasis), `maize` (yellow highlight, sparing use), `outline` (quiet, secondary).
