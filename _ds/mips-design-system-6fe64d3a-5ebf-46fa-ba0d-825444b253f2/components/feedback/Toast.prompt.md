Dismissible toast banner for confirmations and errors.

```jsx
<Toast tone="success">Your enrollment form was submitted!</Toast>
```

Tones: `info`/`success` use navy, `error` uses the reserved red, the only other sanctioned red usage besides form asterisks.
