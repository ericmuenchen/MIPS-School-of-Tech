One-liner: The maize pill divider that marks the start of every section in MIPS documents.

```jsx
<AccentRule />
<h2>Course Description</h2>
```

Sits directly above an `<h2>`. Default width 44px. Decorative only.
